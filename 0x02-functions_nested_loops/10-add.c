#include <stdio.h>
#include "main.h"

/**
* add - the function for this file
* Description - a function that adds two integers and returns the result
* Return: 0 success
*/

int add(int numOne, int numTwo)
{
	return (numOne + numTwo);
}
